import { NocacheDirective } from './nocache.directive';

describe('NocacheDirective', () => {
  it('should create an instance', () => {
    const directive = new NocacheDirective();
    expect(directive).toBeTruthy();
  });
});
